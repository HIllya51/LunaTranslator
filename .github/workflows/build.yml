name: build

on:
  push:
    paths: [ '.github/workflows/build.yml','py/**']
  pull_request:
    paths: [ '.github/workflows/build.yml','py/**']

jobs:
  buildluna:
    uses: ./.github/workflows/buildluna.yml
  