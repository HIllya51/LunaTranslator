name: test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      issues: read
    steps:
       - run: |
          curl --request GET \
          --url https://lunatranslator.org/githubupdatenotify \
          --header 'updatenotifytoken: ${{ secrets.updatenotifytoken }}'